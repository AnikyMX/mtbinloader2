[package]
name = "mtbinloader2"
version = "0.1.12"
edition = "2021"

[dependencies]
android_logger = { version = "0.15.0", default-features = false }
atoi = "2.0.0"
bhook = { version = "0.1.0", git = "https://github.com/mcbegamerxx954/bhook" }
bstr = "1.12.0"
ctor = "0.4.1"
cxx = "1.0.128"
jni = "0.21.1"
libc = "0.2.159"
log = "0.4.22"
materialbin = { version = "0.1.1", git = "https://github.com/mcbegamerxx954/materialbin" }
memchr = "2.7.5"
ndk = "0.9.0"
ndk-sys = "0.6.0"
once_cell = "1.20.2"
page_size = "0.6.0"
plt-rs = "0.3.0"
region = "3.0.2"
scroll = "0.12.0"
tinypatscan = { git = "https://github.com/mcbegamerxx954/tinypatscan", version = "0.1.1" }

[profile.release]
# This lib gets called from nowhere and so unwinding = crash
panic = "abort"
opt-level = 3       # Level 3: Kecepatan Maksimal (Compiler bakal kerja keras)
lto = true          # Link Time Optimization: Gabungin semua kode biar mulus (PENTING buat kurangin stutter)
codegen-units = 1   # Bikin satu unit kode utuh. Build lebih lama, tapi performa in-game lebih stabil.
strip = true        # Buang sampah simbol debug. File .so jadi enteng.
debug = false       # Matikan debug info total.

# [profile.release]
# This lib gets called from nowhere and so unwinding = crash
# panic = "abort"

[profile.githubci]
inherits = "release"
strip = true
lto = true 
codegen-units = 1 
debug = false
panic = "abort"

[lib]
crate-type = ["cdylib"]

[build-dependencies]
cc = "1.1.24"
[lints.clippy]
indexing_slicing = "deny"
unwrap_used = "deny"
